"use client";


export default function ReduxHydrator({ token }: { token: string; }) {

    console.log(token);

    // useEffect(() => {
    //     dispatch(setTokens({ access_token: token, user }));
    // }, [dispatch, token, user]);

    return null;
}
